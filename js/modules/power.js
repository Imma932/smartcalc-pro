// power.js — power and root functions

export const powerModule = {
  sqrt(x) { return Math.sqrt(x); },
  pow(x, y) { return Math.pow(x, y); },
  pow10(x) { return Math.pow(10, x); },
  cube(x) { return Math.pow(x, 3); }
};